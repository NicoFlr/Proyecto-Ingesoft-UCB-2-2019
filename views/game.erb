<%# INSERTAR TODA LA LOGICA / TRAER LA LOGICA DEL JUEGO AQUI? %>
Con una arena de <%= @instruction.get_field.get_rows %> filas y <%= @instruction.get_field.get_columns %> columnas,
 el vehiculo empezo en las coordenadas [<%= @initial_coordinate_in_X%>,<%= @initial_coordinate_in_Y%>],
 y termino en las coordenadas [<%= @instruction.vehicle.get_x_position %>,<%= @instruction.vehicle.get_y_position %>],
apuntando hacia el <%= @instruction.vehicle.get_orientation %>
